<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="LLMClient.Views.ModelConfigurationPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:LLMClient.Models"
             xmlns:converters="clr-namespace:LLMClient.Converters"
             Title="Konfiguracja Modeli AI"
             BackgroundColor="{AppThemeBinding Light={StaticResource BackgroundLight}, Dark={StaticResource BackgroundDark}}">

    <ContentPage.Resources>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter" />
        <converters:IsNotNullConverter x:Key="IsNotNullConverter" />
        <converters:IsNotNullOrEmptyConverter x:Key="IsNotNullOrEmptyConverter" />
        <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
    </ContentPage.Resources>

    <Grid Padding="16,20">
        <!-- Visual State Manager dla responsywności -->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup Name="AdaptiveStates">
                <VisualState Name="Desktop">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1200" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter TargetName="MainGrid" Property="Grid.ColumnDefinitions" Value="2*,3*" />
                        <Setter TargetName="ModelsPanel" Property="Grid.Column" Value="0" />
                        <Setter TargetName="ModelsPanel" Property="Margin" Value="0,0,12,0" />
                        <Setter TargetName="FormPanel" Property="Grid.Column" Value="1" />
                        <Setter TargetName="FormPanel" Property="Margin" Value="12,0,0,0" />
                        <Setter TargetName="MobileHeader" Property="IsVisible" Value="False" />
                        <Setter TargetName="MobileBackButton" Property="IsVisible" Value="False" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState Name="Tablet">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="700" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter TargetName="MainGrid" Property="Grid.ColumnDefinitions" Value="1*,1.5*" />
                        <Setter TargetName="ModelsPanel" Property="Grid.Column" Value="0" />
                        <Setter TargetName="ModelsPanel" Property="Margin" Value="0,0,8,0" />
                        <Setter TargetName="FormPanel" Property="Grid.Column" Value="1" />
                        <Setter TargetName="FormPanel" Property="Margin" Value="8,0,0,0" />
                        <Setter TargetName="MobileHeader" Property="IsVisible" Value="False" />
                        <Setter TargetName="MobileBackButton" Property="IsVisible" Value="False" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState Name="Mobile">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter TargetName="MainGrid" Property="Grid.ColumnDefinitions" Value="*" />
                        <Setter TargetName="ModelsPanel" Property="Grid.Column" Value="0" />
                        <Setter TargetName="ModelsPanel" Property="Margin" Value="0" />
                        <Setter TargetName="FormPanel" Property="Grid.Column" Value="0" />
                        <Setter TargetName="FormPanel" Property="Margin" Value="0" />
                        <Setter TargetName="MobileHeader" Property="IsVisible" Value="True" />
                        <Setter TargetName="MobileBackButton" Property="IsVisible" Value="True" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackLayout Grid.Row="0" Spacing="16" Margin="0,0,0,20">
            <Label Text="Konfiguracja Modeli AI"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />

            <BoxView HeightRequest="2"
                     BackgroundColor="{StaticResource Primary}"
                     CornerRadius="1" />

            <!-- Settings Row -->
            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray800}}"
                    StrokeShape="RoundRectangle 12"
                    StrokeThickness="0"
                    Padding="16,12">
                <Grid ColumnSpacing="24">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackLayout Grid.Column="0" Orientation="Horizontal" Spacing="12">
                        <Label Text="Streaming:"
                               TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}"
                               VerticalOptions="Center"
                               FontSize="14" />
                        <Switch IsToggled="{Binding StreamingEnabled}"
                                OnColor="{StaticResource Primary}" />
                    </StackLayout>

                    <StackLayout Grid.Column="1" Orientation="Horizontal" Spacing="12">
                        <Label Text="Jasny motyw:"
                               TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}"
                               VerticalOptions="Center"
                               FontSize="14" />
                        <Switch IsToggled="{Binding IsLightTheme}"
                                OnColor="{StaticResource Primary}" />
                    </StackLayout>
                </Grid>
            </Border>
        </StackLayout>

        <!-- Mobile Header -->
        <StackLayout x:Name="MobileHeader"
                     Grid.Row="1"
                     Orientation="Horizontal"
                     IsVisible="False"
                     Margin="0,0,0,12">

            <Button x:Name="MobileBackButton"
                    Text="← Lista"
                    BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                    TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}"
                    CornerRadius="10"
                    FontSize="14"
                    Padding="16,8"
                    IsVisible="False"
                    Clicked="MobileBackButton_Clicked" />

            <Label Text="{Binding SelectedModelForEdit.Name, StringFormat='Edytuj: {0}'}"
                   TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"
                   FontSize="16"
                   FontAttributes="Bold"
                   VerticalOptions="Center"
                   Margin="16,0,0,0" />
        </StackLayout>

        <!-- Main Content -->
        <Grid x:Name="MainGrid" Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Panel z listą modeli -->
            <Border x:Name="ModelsPanel"
                    Grid.Column="0"
                    BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}"
                    StrokeShape="RoundRectangle 16"
                    StrokeThickness="0"
                    Margin="0,0,12,0"
                    Padding="16">

                <Border.Shadow>
                    <Shadow Brush="{StaticResource Gray400}"
                            Offset="0,2"
                            Radius="8"
                            Opacity="0.1" />
                </Border.Shadow>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0"
                           Text="Dostępne Modele"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"
                           Margin="0,0,0,16" />

                    <!-- Komunikat gdy brak modeli -->
                    <StackLayout Grid.Row="1"
                                 IsVisible="{Binding Models, Converter={StaticResource CollectionEmptyConverter}}"
                                 VerticalOptions="Center"
                                 HorizontalOptions="Center"
                                 Spacing="12">

                        <Border WidthRequest="64"
                                HeightRequest="64"
                                StrokeShape="RoundRectangle 32"
                                StrokeThickness="0"
                                BackgroundColor="{StaticResource Gray200}"
                                HorizontalOptions="Center">
                            <Label Text="&#xea3b;"
                                   FontFamily="MaterialSymbols"
                                   FontSize="28"
                                   TextColor="{StaticResource Gray500}"
                                   HorizontalOptions="Center"
                                   VerticalOptions="Center" />
                        </Border>

                        <Label Text="Brak modeli AI"
                               FontSize="16"
                               FontAttributes="Bold"
                               HorizontalOptions="Center"
                               TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}" />

                        <Label Text="Dodaj pierwszy model aby rozpocząć!"
                               FontSize="13"
                               HorizontalOptions="Center"
                               TextColor="{StaticResource Gray500}"
                               HorizontalTextAlignment="Center" />
                    </StackLayout>

                    <CollectionView Grid.Row="1"
                                    ItemsSource="{Binding Models}"
                                    SelectedItem="{Binding SelectedModelForEdit}"
                                    SelectionMode="Single"
                                    IsVisible="{Binding Models, Converter={StaticResource CollectionNotEmptyConverter}}">

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid Padding="4" BackgroundColor="Transparent">
                                    <Grid.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.SelectModelCommand}"
                                                              CommandParameter="{Binding .}" />
                                    </Grid.GestureRecognizers>

                                    <Border BackgroundColor="{Binding IsActive, Converter={StaticResource BoolToColorConverter}}"
                                            StrokeShape="RoundRectangle 12"
                                            StrokeThickness="0"
                                            Padding="14,12">

                                        <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <Label Grid.Row="0"
                                                   Text="{Binding Name}"
                                                   TextColor="White"
                                                   FontAttributes="Bold"
                                                   FontSize="14" />

                                            <Label Grid.Row="1"
                                                   Text="{Binding ProviderName}"
                                                   TextColor="#FFFFFFCC"
                                                   FontSize="12"
                                                   Margin="0,2,0,0" />

                                            <Label Grid.Row="2"
                                                   Text="{Binding ModelId}"
                                                   TextColor="#FFFFFF99"
                                                   FontSize="11"
                                                   Margin="0,2,0,0" />
                                        </Grid>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <Button Grid.Row="2"
                            Text="Nowy Model"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            CornerRadius="12"
                            FontSize="14"
                            HeightRequest="48"
                            Margin="0,16,0,0"
                            Command="{Binding AddNewModelCommand}">
                        <Button.ImageSource>
                            <FontImageSource FontFamily="MaterialSymbols"
                                           Glyph="&#xe145;"
                                           Color="White"
                                           Size="18" />
                        </Button.ImageSource>
                    </Button>
                </Grid>
            </Border>

            <!-- Panel z formularzem edycji -->
            <Border x:Name="FormPanel"
                    Grid.Column="1"
                    BackgroundColor="{AppThemeBinding Light={StaticResource SurfaceLight}, Dark={StaticResource SurfaceDark}}"
                    StrokeShape="RoundRectangle 16"
                    StrokeThickness="0"
                    Margin="12,0,0,0"
                    Padding="20">

                <Border.Shadow>
                    <Shadow Brush="{StaticResource Gray400}"
                            Offset="0,2"
                            Radius="8"
                            Opacity="0.1" />
                </Border.Shadow>

                <ScrollView>
                    <StackLayout Spacing="20" IsVisible="{Binding SelectedModelForEdit, Converter={StaticResource IsNotNullConverter}}">

                        <Label Text="Edytuj Model"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}" />

                        <!-- Nazwa -->
                        <StackLayout Spacing="8">
                            <Label Text="Nazwa" TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}" FontSize="13" FontAttributes="Bold" />
                            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray700}}"
                                    StrokeShape="RoundRectangle 10"
                                    StrokeThickness="0"
                                    Padding="12,0">
                                <Entry Text="{Binding SelectedModelForEdit.Name}"
                                       BackgroundColor="Transparent"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"
                                       PlaceholderColor="{StaticResource Gray500}"
                                       Placeholder="np. GPT-4 Turbo"
                                       FontSize="14" />
                            </Border>
                        </StackLayout>

                        <!-- Dostawca -->
                        <StackLayout Spacing="8">
                            <Label Text="Dostawca" TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}" FontSize="13" FontAttributes="Bold" />
                            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray700}}"
                                    StrokeShape="RoundRectangle 10"
                                    StrokeThickness="0"
                                    Padding="12,0">
                                <Picker ItemsSource="{Binding AvailableProviders}"
                                        SelectedItem="{Binding SelectedModelForEdit.Provider}"
                                        TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"
                                        FontSize="14" />
                            </Border>
                        </StackLayout>

                        <!-- Model ID -->
                        <StackLayout Spacing="8">
                            <Label Text="Model ID" TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}" FontSize="13" FontAttributes="Bold" />
                            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray700}}"
                                    StrokeShape="RoundRectangle 10"
                                    StrokeThickness="0"
                                    Padding="12,0">
                                <Entry Text="{Binding SelectedModelForEdit.ModelId}"
                                       BackgroundColor="Transparent"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"
                                       PlaceholderColor="{StaticResource Gray500}"
                                       Placeholder="np. gpt-4-turbo-preview"
                                       FontSize="14" />
                            </Border>
                        </StackLayout>

                        <!-- API Key -->
                        <StackLayout Spacing="8">
                            <Label Text="API Key" TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}" FontSize="13" FontAttributes="Bold" />
                            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray700}}"
                                    StrokeShape="RoundRectangle 10"
                                    StrokeThickness="0"
                                    Padding="12,0">
                                <Entry Text="{Binding SelectedModelForEdit.ApiKey}"
                                       IsPassword="True"
                                       BackgroundColor="Transparent"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"
                                       PlaceholderColor="{StaticResource Gray500}"
                                       Placeholder="Wklej swój API key"
                                       FontSize="14" />
                            </Border>
                        </StackLayout>

                        <!-- Endpoint (dla OpenAI Compatible) -->
                        <StackLayout Spacing="8" IsVisible="{Binding SelectedModelForEdit.RequiresEndpoint}">
                            <Label Text="Endpoint URL" TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}" FontSize="13" FontAttributes="Bold" />
                            <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray700}}"
                                    StrokeShape="RoundRectangle 10"
                                    StrokeThickness="0"
                                    Padding="12,0">
                                <Entry Text="{Binding SelectedModelForEdit.Endpoint}"
                                       BackgroundColor="Transparent"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray900}, Dark={StaticResource White}}"
                                       PlaceholderColor="{StaticResource Gray500}"
                                       Placeholder="https://api.example.com/v1"
                                       FontSize="14" />
                            </Border>
                        </StackLayout>

                        <!-- Opcje -->
                        <Border BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray800}}"
                                StrokeShape="RoundRectangle 12"
                                StrokeThickness="0"
                                Padding="16">
                            <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" ColumnSpacing="16" RowSpacing="12">

                                <StackLayout Grid.Column="0" Grid.Row="0" Orientation="Horizontal" Spacing="12">
                                    <Switch IsToggled="{Binding SelectedModelForEdit.IsActive}"
                                            OnColor="{StaticResource Primary}" />
                                    <Label Text="Aktywny"
                                           TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}"
                                           FontSize="14"
                                           VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Grid.Column="1" Grid.Row="0" Orientation="Horizontal" Spacing="12">
                                    <Switch IsToggled="{Binding SelectedModelForEdit.SupportsStreaming}"
                                            OnColor="{StaticResource Primary}" />
                                    <Label Text="Streaming"
                                           TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}"
                                           FontSize="14"
                                           VerticalOptions="Center" />
                                </StackLayout>

                                <StackLayout Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="12">
                                    <Switch IsToggled="{Binding SelectedModelForEdit.SupportsImages}"
                                            OnColor="{StaticResource Primary}" />
                                    <Label Text="Obsługuje obrazki"
                                           TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}"
                                           FontSize="14"
                                           VerticalOptions="Center" />
                                </StackLayout>
                            </Grid>
                        </Border>

                        <!-- Przyciski -->
                        <Grid ColumnDefinitions="*,*,*" ColumnSpacing="12" Margin="0,8,0,0">
                            <Button Grid.Column="0"
                                    Text="Zapisz"
                                    BackgroundColor="{StaticResource Secondary}"
                                    TextColor="White"
                                    CornerRadius="12"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    HeightRequest="48"
                                    Command="{Binding SaveModelCommand}" />

                            <Button Grid.Column="1"
                                    Text="Test"
                                    BackgroundColor="{StaticResource Primary}"
                                    TextColor="White"
                                    CornerRadius="12"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    HeightRequest="48"
                                    Command="{Binding TestModelCommand}" />

                            <Button Grid.Column="2"
                                    Text="Usuń"
                                    BackgroundColor="{StaticResource Error}"
                                    TextColor="White"
                                    CornerRadius="12"
                                    FontSize="14"
                                    FontAttributes="Bold"
                                    HeightRequest="48"
                                    Command="{Binding DeleteModelCommand}" />
                        </Grid>

                        <!-- Status -->
                        <Label Text="{Binding StatusMessage}"
                               TextColor="{Binding StatusColor}"
                               FontSize="13"
                               FontAttributes="Bold"
                               IsVisible="{Binding StatusMessage, Converter={StaticResource IsNotNullOrEmptyConverter}}"
                               Margin="0,8,0,0"
                               HorizontalTextAlignment="Center" />
                    </StackLayout>
                </ScrollView>
            </Border>
        </Grid>

        <!-- Footer -->
        <Button Grid.Row="3"
                Text="Zamknij"
                BackgroundColor="{AppThemeBinding Light={StaticResource Gray200}, Dark={StaticResource Gray700}}"
                TextColor="{AppThemeBinding Light={StaticResource Gray700}, Dark={StaticResource Gray300}}"
                CornerRadius="12"
                FontSize="14"
                HeightRequest="48"
                WidthRequest="120"
                Margin="0,20,0,0"
                Command="{Binding CloseCommand}"
                HorizontalOptions="End" />
    </Grid>

</ContentPage>
